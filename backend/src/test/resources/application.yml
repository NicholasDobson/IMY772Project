spring:
  datasource:
    url: jdbc:tc:postgresql:15-alpine:///test_db
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: create-drop # Creates fresh tables for testing
    show-sql: true