# South Africa province boundaries

`sa-provinces.geojson` is the simplified ADM1 layer from [geoBoundaries](https://www.geoboundaries.org/) (ZAF, CC BY 4.0).

Used by the map view for provincial overlays.
